{
  "name": "wordlemusic",
  "version": "1.0.0",
  "private": true,
  "description": "Song Guesser - A Heardle-style daily music guessing game",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix server",
    "start": "node server/index.js",
    "seed": "npm run seed --prefix server",
    "migrate": "npm run migrate --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
